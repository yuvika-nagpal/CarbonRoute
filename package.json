{
  "name": "carbonroute-monorepo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "localtunnel": "^2.0.2",
    "untun": "^0.2.2"
  }
}
